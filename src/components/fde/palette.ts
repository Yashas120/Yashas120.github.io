/**
 * /fde reads as a field deployment workbench rather than a control plane, so it
 * takes a warm accent instead of the backend endpoint's cool blue. Constraint
 * violet and outcome green stay distinct from the accent at a glance.
 */
export const ACCENT = "#fb923c";
export const GREEN = "#4ade80";
export const VIOLET = "#a78bfa";
export const RED = "#f87171";
export const AMBER = "#fbbf24";
